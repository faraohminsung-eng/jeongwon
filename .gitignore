# Node / Next.js
node_modules/
.next/
out/
build/
*.tsbuildinfo
next-env.d.ts

# 환경변수 (절대 커밋하지 않음 — PG사 키, DB 접속정보 등)
.env
.env*.local

# Prisma
prisma/*.db

# Design canvas의 seed 산출물 (Main.dc.html + canvas.json에서 재생성 가능)
design/*-landing.html
design/*.seeded.html

# OS
Thumbs.db
.DS_Store

/lib/generated/prisma

# 로컬 디스크 저장소 어댑터가 업로드한 사진 (S3 미설정 시). 실사진은 저장소에 커밋하지 않습니다.
/data/uploads

# Supabase CLI 로컬 캐시 (프로젝트 연결 정보 등, 실제 마이그레이션은 Prisma로 관리)
/supabase/.temp
.vercel
